import { slider } from '../components/slider.js';
import { header } from '../components/header.js';

export const home = () => {
  // Initialize Header
  header('home');

  // Initialize Slider
  slider();
};
